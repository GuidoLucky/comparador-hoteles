[phases.setup]
nixPkgs = ["nodejs_20", "chromium", "nss", "at-spi2-atk", "cups", "xorg.libXcomposite", "xorg.libXdamage", "xorg.libXext", "xorg.libXrandr", "mesa", "libgbm", "alsa-lib"]

[phases.install]
cmds = ["npm install", "npx playwright install chromium"]

[start]
cmd = "npm start"
