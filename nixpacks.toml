[phases.setup]
nixPkgs = ["nodejs_20", "chromium", "nss", "at-spi2-atk", "atk", "cups", "xorg.libXcomposite", "xorg.libXdamage", "xorg.libXext", "xorg.libXrandr", "libgbm", "alsa-lib", "pango", "cairo", "glib"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "npm start"
